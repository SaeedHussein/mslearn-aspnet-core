apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: identity
  labels:
    app: eshop
    service: identity
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /identity-api
        pathType: Prefix
        backend:
          service:
            name: identity-api
            port:
              number: 80
