apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: catalog
  labels:
    app: eshop
    service: catalog
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /catalog-api
        pathType: Prefix
        backend:
          service:
            name: catalog-api
            port:
              number: 80
